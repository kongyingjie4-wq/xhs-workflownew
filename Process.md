# 小红书文案生成看板 - 项目流程

## 项目概述

这是一个面向母婴领域运营人员的文案生成工具，基于真实用户评论数据，自动生成小红书文案。

---

## 数据格式

系统接收 .jsonl 格式的文件，每行是一个 JSON 对象，包含评论数据。系统只提取其中的 content 字段（评论内容），其他字段忽略。

示例：
```json
{"comment_id": "66aa49fc0000000009002d22", "create_time": 1722436093000, "content": "宝宝三个月，不喜欢吃奶粉，亲喂只吃前面三分钟，很容易睡着，怎么办？", "nickname": "菜菜小馄饨"}
```

---

## 全自动流程

### 第一步：导入数据

用户上传 .jsonl 文件，系统解析文件，提取 content 字段，统计总行数、有效行数、无效行数。这一步是纯代码处理，不调用大模型。

### 第二步：痛点统计

系统使用预定义的痛点关键词词典对评论内容进行匹配，统计高频词。词典里定义了母婴领域的痛点词汇，比如追奶、堵奶、母乳喂养、乳头皲裂、奶量不足、回奶、乳腺炎、呛奶、吐奶等。系统遍历每条评论，检查是否包含词典里的关键词，然后统计每个痛点出现的次数。这一步是纯代码处理，不调用大模型。

### 第三步：一键生成

用户点击「一键生成文案」按钮，系统自动完成以下操作：

1. 自动选择主题：从高频词中随机选择 1 到 3 个作为文案主题
2. 自动选择类型：随机决定生成纯科普文案还是科普加营销文案
3. 调用大模型：把选中的主题、文案类型、产品优势文档、提示词文档组装成 Prompt，发送给 mimo 大模型
4. 流式返回：大模型通过 SSE 流式返回生成的标题和正文，前端实时显示
5. 展示决策：前端展示 Agent 的决策过程，包括选择了什么主题、什么类型

这一步会调用大模型。

### 第四步：复制使用

用户查看生成的文案，满意后点击「一键复制」按钮，复制文案到剪贴板，然后去小红书发布。

---

## 半自动流程

### 第一步：导入数据

与全自动流程相同。

### 第二步：痛点统计

与全自动流程相同。

### 第三步：选择主题

用户点击「开始分析」按钮，系统展示高频词列表。用户手动选择 1 个或多个高频词作为文案主题。这一步是纯代码处理，不调用大模型。

### 第四步：选择类型

用户选择文案类型，纯科普文案或科普加营销文案。

### 第五步：生成文案

用户点击「生成文案」按钮，系统调用大模型，把选中的主题、文案类型、产品优势文档、提示词文档组装成 Prompt，发送给 mimo 大模型。大模型通过 SSE 流式返回生成的标题和正文，前端实时显示。这一步会调用大模型。

### 第六步：微调文案

如果用户对文案不满意，可以点击「微调文案」按钮，输入修改意见，比如"把第二段写得更温柔一点"。系统调用大模型，把当前文案和修改意见组装成 Prompt，发送给 mimo 大模型，大模型返回修改后的文案。用户可以多次微调，直到满意为止。这一步会调用大模型。

### 第七步：复制使用

与全自动流程相同。

---

## 大模型调用总结

| 步骤 | 是否调用大模型 | 说明 |
|------|---------------|------|
| 导入数据 | 否 | 纯代码解析 .jsonl 文件 |
| 痛点统计 | 否 | 纯代码，使用关键词词典匹配 |
| 选择主题 | 否 | 纯代码，随机选择或用户手动选择 |
| 生成文案 | 是 | 调用 mimo 大模型 API |
| 微调文案 | 是 | 调用 mimo 大模型 API |

---

## 痛点词典

系统使用预定义的痛点关键词词典进行匹配，词典里定义了母婴领域的痛点词汇，包括同义词。比如"追奶"、"奶量不足"、"奶少"、"下奶"、"催奶"、"奶水少"、"奶水不够"、"没奶"、"回奶"、"奶量"都会被统计到"追奶"这个痛点类别里。

---

## 系统设置

系统支持配置以下内容：

1. API Key 管理：添加、删除、激活大模型 API Key，支持 mimo 和 openai 两种模型
2. 产品优势配置：编辑产品优势文档，用于科普加营销文案生成
3. 提示词配置：编辑提示词文档，用于指导大模型生成文案

---

## 技术栈

- 后端：FastAPI + SSE 流式响应
- 前端：原生 HTML/CSS/JavaScript + Bootstrap 5
- AI：mimo 大模型 API
- 分词：jieba 中文分词库（用于文本清洗）
