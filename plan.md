 一、全局系统设置（静态配置中心）
在设置页面中，配置以下 3 个静态配置项：

大模型配置 (LLM Config)：

模型种类选择：前端下拉菜单（如：DeepSeek-V3、Claude-3.5-Sonnet等）。

API Key / Token 输入框：用于后端接口鉴权。

产品优势点配置 (product_advantages.md)：

内容：以 Markdown 格式存放我们品牌吸奶器的核心卖点和技术参数（支持前端直接编辑保存）。

小红书违禁词配置 (banned_words.md)：

内容：以 Markdown 格式存放严禁出现的广告法违禁词和营销黑话。

 二、核心四步走流水线（SOP）
第一阶段：本地数据流导入 (Data Ingestion)
输入端规约：100% 只接收本地上传的 .jsonl 文件（一行一个标准的 JSON 对象，格式形如 {"note_id": "...", "content": "..."}）。

看板形态：一个简单的文件拖拽/上传组件。

第二阶段：大模型分析与智能话题提炼 (Topic Library)
后端清洗与提取：

后端（Python）读取上传的 .jsonl 文件，循环解析每一行，精准把里面的 content 字段的值全部拿出来，拼接成一个纯文本长字符串。

用正则代码洗掉文本里无意义的干扰乱码或死链接。

第一次调用大模型（分析阶段）：

后端将清洗后的纯文本内容打包，调用在【设置】里选定的大模型。

Prompt 目标：要求大模型分析这些宝妈评论的纯文本，找出高频痛点（如吸力痛、呛奶），并提炼出 5 个高转化的文案主题大纲返回给前端。

看板形态：前端渲染出大模型返回的 5 个主题选题卡片，运营人员在卡片上点击勾选确认。

第三阶段：文案类型决策与初次文案生成 (Route & Generation)
接口设计（枚举单选）：当运营人员确认选题后，前端调用一个带枚举值的路由选项接口：

route_type = "KIND_REMINDER"（纯科普路线：100% 只输出科学喂养知识，绝对不提品牌）。

route_type = "MARKETING"（营销路线：前置科普建信任，后置必须自然融入设置里的 product_advantages.md 内容进行推销）。

第二次调用大模型（初次生成阶段）：

用户选完枚举值并点击生成按钮，系统立刻触发第二次大模型调用。

后端把【选定的话题】+【枚举值路由提示词】+【设置里的优势点与违禁词.md】作为 Context 一并投喂给大模型。

看板形态：大模型通过 Stream（流式）在前端大文本框里，吐出初版的标题和正文。

第四阶段：AI 场景文案工作室与无限次自然语言微调 (Writing Studio)
业务场景：营销人员看着大文本框里的初版文案，如果不满意，直接在下方的 Chat 对话框输入修改意见（例如：“让语气更温柔一点” / “把第三段缩短，加点爆款Emoji”）。

第 N 次调用大模型（局部微调无状态循环）：

用户点击发送，系统触发下一次大模型调用。

后端将【当前大文本框里最新的文本】+【本次运营人员的修改意见】（如果想要效果更好，前端可以带上之前的简短对话 History）打包发送给大模型。

大模型根据指令对文本进行实时微调和重新润色，并将新结果重新流式渲染回前端大文本框。

此过程支持选择调整很多次（调用很多次），直到营销人员觉得满意为止。

结束与分发：整个微调过程无需保存到数据库（不占用持久化存储）。营销人员觉得文案完美后，直接一键复制这个标题和文案，自己去小红书上传发布，系统任务当即无痕结束。

 独立开发、跑通闭环的核心工程建议：
关于第四阶段“不保存”的内存设计：
既然不需要保存到数据库，你的前端页面只需要维持一个 current_text 的变量。每次运营人员输入修改意见时，就把这个变量里的文本传给后端。后端让大模型针对这段文本和意见进行重写，再把新文本返回给前端，覆盖 current_text 即可。这能让你的数据库非常清爽，毫无存储压力。

前端 UI 的复制按钮：
在大文本框旁边加一个大大的 [一键复制文案] 按钮。调用浏览器的 navigator.clipboard.writeText 接口，营销人员点击后直接去小红书网页端或 App 里 Ctrl+V 就能发帖，真正做到“随用随走，高效至上”。